{
  "rules": {
    "groups": {
      "$groupId": {
        ".read": "true",
        ".write": "true",
        "adminTokenHash": {
          ".write": "!data.exists()"
        },
        "id": {
          ".write": "!data.exists()"
        },
        "createdAt": {
          ".write": "!data.exists()"
        },
        "participants": {
          ".read": "true",
          "$participantId": {
            ".write": "true"
          }
        }
      }
    }
  }
}